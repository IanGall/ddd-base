# ddd-base-bom 模块协作说明

## 模块定位
- 本模块作为 ddd 基础平台 BOM，统一导出基础依赖组合。
- 负责对内提供稳定依赖入口，减少重复声明。

## 变更边界
- 允许修改：BOM 导入关系、基础依赖编排、版本继承策略。
- 禁止修改：业务实现代码与具体运行逻辑。

## 依赖约束
- 优先通过 BOM 管理版本，不在下游模块分散声明版本。
- 变更需保持依赖收敛，避免出现多版本冲突。

## 提交前检查
- 核对导入链路是否完整且无循环引用。
- 验证关键依赖坐标与版本是否符合基线。
- 删除无效导入项与不再使用的 BOM 配置。

# ddd-base 总工程协作说明

## 模块定位
- 本目录是 ddd-base 聚合根工程，负责统一模块编排与版本基线。
- 聚焦基础能力拆分、构建入口管理与跨模块一致性约束。

## 变更边界
- 允许修改：模块聚合关系、父级构建配置、全局约束声明。
- 禁止修改：与当前任务无关的具体业务实现代码。

## 依赖约束
- 通过聚合模块管理公共能力边界，避免跨层直接耦合。
- 保持模块职责单一，避免在根工程沉淀业务逻辑。

## 提交前检查
- 确认模块清单完整且构建入口无遗漏。
- 评估变更是否会引入循环依赖或版本漂移风险。
- 删除无用配置与失效说明，保持工程结构整洁。
